{
  "name": "@regents/platform-learn-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build && pagefind --site dist && node scripts/sync-to-phx.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "0.38.4",
    "astro": "6.1.10",
    "pagefind": "^1.4.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
